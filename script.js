//let index = 0;

/**function addTasks() {
    let adtasks = $('#inp_task').val();


    let list = $("li").attr({
        class: "listItems",
    });
    let checkboxs = $("input").attr({
        type: "checkbox",
        id: "inputCheck",

    });
    let labels = $("label").attr({
        id: "inputLabel",
    });
    let editInput = $("input").attr({
        type: "Text",
        id: "editTextbox"
    });

    let editButton = $().attr({
        id: "editButton",
        onclick: editThis(this),
        value: "Edit",
    });
    let deleteButton = $("button").attr({
        id: "deletebtn",
        onclick: deleteThis(this),
        Value: "Delete"
    });

    $("ul").append(list, checkboxs, labels, editInput, editButton, deleteButton);


}**/